tasks:
  - init: pnpm install 

github:
  prebuilds:
    master: true
    branches: true
    pullRequests: true
    pullRequestsFromForks: true
    addCheck: true
    addComment: true
    addBadge: false

image:
  file: .gitpod/.gitpod.dockerfile
